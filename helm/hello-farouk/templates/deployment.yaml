apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "hello-farouk.fullname" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "hello-farouk.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "hello-farouk.name" . }}
    spec:
      containers:
        - name: hello-farouk
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          ports:
            - containerPort: 80